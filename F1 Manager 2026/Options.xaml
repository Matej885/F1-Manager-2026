<Window x:Class="F1_Manager_2026.Options"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Options" 
        Height="1080" Width="1920"
        WindowStyle="None"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Icon="/Images/Program_icon.png"
        MouseMove="Window_MouseMove">
    <Window.Resources>
        <Style x:Key="F1MenuButton" TargetType="Button">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Bahnschrift SemiBold"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="35,0,0,0"/>
            <Setter Property="Margin" Value="0,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#E10600"/>
                                <Setter Property="Foreground" Value="#E10600"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NextButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="0,10,10,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#252525"/>
                                <Setter Property="Foreground" Value="#E10600"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#0F0F0F" ClipToBounds="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image Grid.RowSpan="2" Name="Backgrounf_Image" 
               Source="/Images/Backgrounf_Image.png" 
               Stretch="UniformToFill" Opacity="0.55" RenderTransformOrigin="0.5,0.5">
            <Image.RenderTransform>
                <TransformGroup>
                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                    <TranslateTransform x:Name="BackgroundTranslate"/>
                </TransformGroup>
            </Image.RenderTransform>
        </Image>

        <Image Grid.RowSpan="2" Name="DynamicCar_Image" Stretch="UniformToFill" Visibility="Collapsed" Opacity="0.75" RenderTransformOrigin="0.5,0.5">
            <Image.RenderTransform>
                <TransformGroup>
                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                    <TranslateTransform x:Name="DynamicCarTranslate"/>
                </TransformGroup>
            </Image.RenderTransform>
        </Image>

        <Rectangle Grid.RowSpan="2" IsHitTestVisible="False">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#F50F0F0F" Offset="0.0"/>
                    <GradientStop Color="#900F0F0F" Offset="0.4"/>
                    <GradientStop Color="#B50F0F0F" Offset="1.0"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <Border Grid.Row="0" Background="#1A1A1A" BorderBrush="#E10600" BorderThickness="0,0,0,3" Panel.ZIndex="10">
            <Grid Margin="40,0">
                <StackPanel Orientation="Horizontal">
                    <Image Name="TeamLogo_Image" Width="60" Margin="0,0,20,0" Stretch="Uniform"/>
                    <Label Content="F1 MANAGER 2026" Foreground="White" FontSize="32" 
                           VerticalAlignment="Center" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="80,0,0,120" Width="420">
                <Button x:Name="Continue_Button" Content="CONTINUE" Height="75" Style="{StaticResource F1MenuButton}" MouseEnter="Continue_Button_MouseEnter" Click="Continue_Button_Click"/>
                <Button x:Name="Career_Create_Button" Content="NEW CAREER" Height="75" Style="{StaticResource F1MenuButton}" MouseEnter ="Continue_Button_MouseEnter" Click="Career_Create_Button_Click"/>
            </StackPanel>

            <Border Name="ContinueInfo_Card" HorizontalAlignment="Left" VerticalAlignment="Top" 
                    Margin="80,40,0,0" Width="420" Background="#161616" CornerRadius="12" 
                    BorderBrush="#333" BorderThickness="1.5" Padding="25" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="CURRENT CAREER PROGRESS" Foreground="#E10600" FontSize="11" FontWeight="Bold" FontFamily="Bahnschrift SemiBold"/>

                    <TextBlock Name="SavedTeamName_Label" Text="TEAM NAME" Foreground="White" FontSize="26" FontWeight="Bold" Margin="0,8,0,2" FontFamily="Bahnschrift SemiBold" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Name="SavedSeason_Label" Text="DAY 1" Foreground="#888" FontSize="14" Margin="0,0,0,15" FontFamily="Bahnschrift"/>

                    <Separator Background="#252525" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="TOTAL BUDGET" Foreground="#555" FontSize="11" FontWeight="Bold" FontFamily="Bahnschrift"/>
                            <TextBlock Name="SavedBudget_Label" Text="$0" Foreground="#2ECC71" FontSize="18" FontWeight="Bold" FontFamily="Bahnschrift SemiBold"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="NEXT RACE" Foreground="#555" FontSize="11" FontWeight="Bold" FontFamily="Bahnschrift"/>
                            <TextBlock Name="SavedNextRace_Label" Text="NOT FOUND" Foreground="White" FontSize="15" FontWeight="Bold" FontFamily="Bahnschrift SemiBold" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="80,0,0,45">
                <Button Name="Volume_Button" Width="60" Height="60" Click="Button_Click_4" MouseEnter="Continue_Button_MouseEnter" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="volBorder" CornerRadius="8" Background="#1A1A1A" BorderBrush="#333" BorderThickness="2">
                                <Image x:Name="Volume_Icon" Source="/Images/volume_on.png" Margin="12"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="volBorder" Property="BorderBrush" Value="#E10600"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Name="Exit_Button" Width="160" Height="60" Margin="15,0,0,0" Click="Exit_Button_Click" MouseEnter="Continue_Button_MouseEnter" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="exitBorder" CornerRadius="8" Background="#1A1A1A" BorderBrush="#333" BorderThickness="2">
                                <TextBlock Text="EXIT GAME" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" 
                                           FontFamily="Bahnschrift SemiBold" FontSize="18"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="exitBorder" Property="BorderBrush" Value="#E10600"/>
                                    <Setter Property="Foreground" Value="#E10600"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>

            <Border HorizontalAlignment="Right" VerticalAlignment="Bottom" 
                    Margin="0,0,60,45" Width="380" Height="90" 
                    Background="#121212" CornerRadius="12" 
                    BorderBrush="#333" BorderThickness="1.5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="5">
                        <Border CornerRadius="8" ClipToBounds="True">
                            <MediaElement Name="Music_Visualizer" 
                                          Source="Videos/Sound_Wave.mp4" 
                                          LoadedBehavior="Manual" 
                                          Stretch="UniformToFill" 
                                          IsMuted="True" 
                                          MediaEnded="WaveRepeat"/>
                        </Border>
                        <Border CornerRadius="8" Height="77" VerticalAlignment="Top">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="Transparent" Offset="0.5"/>
                                    <GradientStop Color="#121212" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                    </Grid>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                        <TextBlock Text="NOW PLAYING" Foreground="#E10600" FontSize="10" 
                                   FontWeight="Bold" FontFamily="Bahnschrift SemiBold"/>
                        <TextBlock Name="Song_Title_Label" Text="Official F1 Theme" 
                                   Foreground="White" FontSize="15" TextTrimming="CharacterEllipsis"
                                   FontFamily="Bahnschrift SemiBold"/>
                    </StackPanel>

                    <Button Grid.Column="2" Name="Next_Song_Button" 
                            Content="▶" FontSize="20" 
                            Style="{StaticResource NextButtonStyle}"
                            MouseEnter="Continue_Button_MouseEnter"
                            Click="Next_Song_Button_Click" Cursor="Hand"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>