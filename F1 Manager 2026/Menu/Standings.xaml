<Window x:Class="F1_Manager_2026.Menu.Standings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:F1_Manager_2026.Menu"
        mc:Ignorable="d"
        WindowState="Maximized"
        WindowStyle="None"
        Title="Standings" Height="1080" Width="1920"
        Background="#050505">

    <Window.Resources>
        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#111"/>
            <Setter Property="Foreground" Value="#AAA"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#E10600"/>
            <Setter Property="BorderThickness" Value="2,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="25,12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#222"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="DriverCardTemplate">
            <Border Background="#0F0F0F" BorderBrush="#222" BorderThickness="1" Margin="5" Height="280" Width="210">
                <Grid>
                    <Image Source="{Binding TeamLogo}" Opacity="0.08" Margin="30" Stretch="Uniform"/>

                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,12,0">
                        <TextBlock Text="{Binding Skill}" Foreground="#E10600" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right"/>
                        <Rectangle Width="15" Height="2" Fill="#E10600" HorizontalAlignment="Right" Margin="0,2,0,0"/>
                    </StackPanel>

                    <Viewbox Stretch="Uniform" Margin="10,35,10,75">
                        <Grid Width="300" Height="400">
                            <Image Source="{Binding SuitPath}" VerticalAlignment="Bottom" Stretch="Uniform"/>
                            <Image Source="{Binding PhotoPath}" Width="220" VerticalAlignment="Top" 
                                   Margin="0,35,0,0" Stretch="Uniform"/>
                        </Grid>
                    </Viewbox>

                    <Border VerticalAlignment="Bottom" Background="#F20A0A0A" BorderBrush="#333" BorderThickness="0,1,0,0" Padding="5,8">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" Foreground="White" FontSize="12" FontWeight="Black" HorizontalAlignment="Center" TextAlignment="Center"/>
                            <TextBlock Text="{Binding Points, StringFormat={}{0} PTS}" Foreground="#E10600" FontSize="14" FontWeight="ExtraBold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0A0A0A" BorderBrush="#333" BorderThickness="0,0,0,1">
            <Grid Margin="60,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Rectangle Width="5" Height="30" Fill="#E10600" Margin="0,0,15,0"/>
                    <TextBlock Text="DRIVER STANDINGS" 
                               Foreground="White" 
                               FontSize="24" 
                               FontWeight="Black">
                        <TextBlock.LayoutTransform>
                            <ScaleTransform ScaleX="1.05" />
                        </TextBlock.LayoutTransform>
                    </TextBlock>
                    <TextBlock Text=" | SEASON 2026" 
                               Foreground="#666" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               VerticalAlignment="Center" 
                               Margin="10,0,0,0"/>
                </StackPanel>

                <Button Name = "BackButton"  Content="BACK TO HQ" 
                        HorizontalAlignment="Right" 
                        VerticalAlignment="Center" 
                        Style="{StaticResource BackButtonStyle}" 
                        Click="BtnBack_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="60,30" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">

                <StackPanel Margin="10,0">
                    <TextBlock Text="2" Foreground="#AAA" FontSize="20" FontWeight="Black" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    <ContentControl Content="{Binding Top2}" ContentTemplate="{StaticResource DriverCardTemplate}">
                        <ContentControl.LayoutTransform>
                            <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                        </ContentControl.LayoutTransform>
                    </ContentControl>
                </StackPanel>

                <StackPanel Margin="20,0">
                    <TextBlock Text="1" Foreground="#FFD700" FontSize="26" FontWeight="Black" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    <Border BorderBrush="#E10600" BorderThickness="0,0,0,4">
                        <ContentControl Content="{Binding Top1}" ContentTemplate="{StaticResource DriverCardTemplate}">
                            <ContentControl.LayoutTransform>
                                <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                            </ContentControl.LayoutTransform>
                        </ContentControl>
                    </Border>
                </StackPanel>

                <StackPanel Margin="10,0">
                    <TextBlock Text="3" Foreground="#CD7F32" FontSize="20" FontWeight="Black" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    <ContentControl Content="{Binding Top3}" ContentTemplate="{StaticResource DriverCardTemplate}">
                        <ContentControl.LayoutTransform>
                            <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                        </ContentControl.LayoutTransform>
                    </ContentControl>
                </StackPanel>

            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="2" Margin="60,10" VerticalScrollBarVisibility="Hidden">
            <ItemsControl ItemsSource="{Binding OtherDrivers}" ItemTemplate="{StaticResource DriverCardTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="3" Background="#050505"/>
    </Grid>
</Window>